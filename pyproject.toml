[project]
name = "ComfyUI-Sequencer"
version = "1.0.0"
description = "Video Sequencer for ComfyUI — join MP4 clips end-to-end with cuts or dissolves (FFmpeg)."
requires-python = ">=3.10"

[tool.comfy]
PublisherId = "fundy"
DisplayName = "Sequencer"
