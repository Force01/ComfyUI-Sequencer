[project]
name = "ComfyUI-Sequencer"
version = "1.0.0"
description = "Video Sequencer for ComfyUI — join video clips end-to-end with cuts or dissolves (FFmpeg)."
requires-python = ">=3.10"
license = { file = "LICENSE" }

[tool.comfy]
PublisherId = "fundy"
DisplayName = "Sequencer"
